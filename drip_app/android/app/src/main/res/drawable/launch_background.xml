<?xml version="1.0" encoding="utf-8"?>
<!-- Native launch screen: DRIP midnight, so the handoff to the Flutter
     launch sequence (wordmark resolving from blur) is seamless. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/drip_base" />
</layer-list>
